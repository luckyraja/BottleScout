#UI Issues
1. App should open to the camera screen every time. on the bottom right corner, there should be an icon for inventory, on the bottom left there should be an icon that references existing photos. when the users takes a picture of the bottle, it should go to the "bottle" screen. 
2. The bottle Screen should show a streamlined view of info in the bottle. It shoudl have a picture of the bottle. The top right corner should have an icon to depict that the bottle is "owned" or not, some sort of icon to denote the bottle was purcashed. The upper left corner should have a back button. under the picture of the bottle there should be the follow descriptive fields: type - wine/beer/liqour, Price - $Reasonable price range from the LLM. Make sure the LLM uses web search to find a reasonable estimate, Tasting notes - Ask the LLM to use web search to find tasting notes, Pairing notes - Ask the LLM to use web search to find pairing suggesitons. 
3. the last remaining screen should be an inventory. This should be an ordered list, sorted by scan date. There should be a filter for "owned" or not. 